# Debt Pending — product-plan-correction

## To Close
- debt-nested-product-plan-lifetime — this cycle defines a durable, project-lifetime product-plan location (outside any single cycle's dir) that survives child-cycle `dev:done` teardown and is deleted only on project completion, which is exactly this entry's "Done looks like".
