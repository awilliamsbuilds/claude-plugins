# claude-plugins

Personal Claude Code plugin repo for awilliamsbuilds. Registered as the `local-plugins` marketplace via `github` source type in `~/.claude/settings.json`.

**This file is agent-facing configuration, auto-loaded every session.** The human-facing front door — what each plugin is, how to install and enable them, the plugin catalog, and the repo's directory layout — lives in [README.md](README.md); it is the single source of truth for those, so don't duplicate them here. Keep this file to the operational guardrails the agent needs in context and the `/dev` Component Registry below.

## Adding a Plugin or Skill

Use the `add-plugin` skill — it owns the full workflow (the human walkthrough is in [README.md](README.md#adding-a-plugin)). Two guardrails worth having in context:

- Adding a **plugin** touches `plugins/<name>/.claude-plugin/plugin.json`, one `skills/<skill>/SKILL.md`, and a new entry in `.claude-plugin/marketplace.json`. Adding a **skill** to an existing plugin only touches a new `SKILL.md` — skip the plugin.json and marketplace steps.
- Before updating `.claude-plugin/marketplace.json`, read it first (to get its current SHA if editing via the GitHub API) rather than blind-writing it.

## Deploying Changes

Changes must be merged to `main`. After merging, run `/plugin update` in Claude Code.

## Important Notes

- The `directory` source type is broken in Claude Code. This repo uses `github` source — do not change it.
- The `description` field in SKILL.md frontmatter is what Claude Code uses to decide when to invoke a skill. Make it rich with trigger phrases.
- `GITHUB_PERSONAL_ACCESS_TOKEN` must be set in `~/.claude/settings.json` with `repo` scope for this repo.

## Component Registry
*Last updated by /dev · 2026-08-23*

| Component | Path | Purpose |
|-----------|------|---------|
| `dev:autopilot` | plugins/dev/skills/autopilot/SKILL.md | No-gate orchestrator for the /dev workflow; also the handoff target — accepts `/dev:autopilot docs/dev/<feature>/<artifact>.md`, resolves `WORKDIR` itself, and reads `tier`/`skipped[]`/`completed[]` from the resolved `state.json`. Step 3's **Start stage** rule is the single statement of where a run begins: the earliest stage in the selected tier row absent from `completed[]` — **the resolved start stage** — resolved **once**, at the start of the run. `completed[]` is the authority and `stage` only a hint; the skip applies **only ahead of the entry point**, so from there onward every row stage executes even if already in `completed[]` (a non-contiguous `["spec","build"]` resolves to Plan and deliberately re-runs Build). **No stage is exempted, PR included** — and PR is now safely re-enterable, `dev:pr` Step 4 having gained the idempotent-resume rule that closed `debt-autopilot-pr-re-entry-not-idempotent`. Step 1's announce line and the `handoff_at` marker `(writes: autopilot-only)` both derive from that one resolved value, and `handoff_at` is stated **by cause** — the stage `completed[]` does not yet record — covering both an unprompted early invocation and an approved Branch A Spec gate on a UI cycle. Step 4's completion report keys `— already complete` on **position relative to the entry point**, never on `completed[]` membership. The "When autopilot stops" list in `## Purpose` now also names **a challenger dispatch that errored twice at Spec**, beside the build failure and the unrunnable reviewer at Validate. Both challenger-loop sections carry the `applied` / `applied_concerns` split as a **summary restatement** — `dev:spec` Step 12a is canonical, `dev:plan` Step 7a its mirror — and the spec-challenger section restates the errored-dispatch rule's loop-bookkeeping half only (no `loops_run` advance, `null` counters, **one retry per stage — not per round**, the part that only bites in the looping mode), explicitly noting the rule does **not** extend to the plan challenger |
| `dev:build` | plugins/dev/skills/build/SKILL.md | Stage 4 — implements the plan |
| `dev:debt` | plugins/dev/skills/debt/SKILL.md | On-demand backlog + tech debt tracker — `list` (surfaces `promoted` + `routing: pending`, re-attempts stranded deliveries), `show`, `closed`, `close`, `add` (capture verb with `--debt`/`--plugin`/`--repo` overrides + P9 cross-repo routing), `inbox` (drain/convert routed `dev-backlog` issues into the local store; plugin repo only), `view` / `view stop` (Step 9 — launches the detached loopback-only browser viewer via `viewer.py`; exempt from Step 1's store derivation and empty-store stop). Step 6 is **canonical** for closing an item; `references/entry-adapters.md` §A4 mirrors its step 4 for the `/dev:fix backlog` tail, with three divergences named identically at both ends |
| `dev:debt` viewer | plugins/dev/skills/debt/viewer.py · viewer_page.html · test_viewer.py | The `view` verb's runtime — stdlib-only (Python 3.9) front-matter parser, store loader over the active corpus + `closed/` archive, disk-derived facet derivation, page render, `BaseHTTPRequestHandler` server (route allowlist, `127.0.0.1` bind, Host allowlist, identity/pid headers), and the `serve`/`start`/`stop` CLI with probe-based identity on ports 8730–8739. The repo's sole guarded `PRIMARY` derivation outside the skills. `viewer_page.html` is the rail/list/detail shell Milestone 3's `lifecycle-viewer` inherits |
| `dev:dev` | plugins/dev/skills/dev/SKILL.md | Main entry point for the /dev workflow; Step 6 product-plan continuation scopes to the governing plan, else scans `docs/dev/product-plans/` (multi-plan-aware). Also the workflow's **reference surface** since `dev:start` was retired into it: Step 1's `list` argument short-circuits to Step 1a — a read-only reference (registry-read descriptions with a missing-registry fallback, the stage pathway with each `/dev:<stage>` command, the tier rules, and the non-pathway skills) that returns **before** Step 2's init check, so asking for it in an uninitialized repo creates nothing. Step 5's stage sequence is stated as **rules over the pathway**, not enumerated rows — which is what closes the missing Deep + no-ui case |
| `dev:done` | plugins/dev/skills/done/SKILL.md | Stage 7 — merges the PR, checks off the product plan, flushes tech debt, and cleans up the branch, worktree and primary checkout. **No longer writes any reviewable content**: the Component Registry, docs-prose reconciliation, decision log and retrospective all moved to `dev:pr` Step 5a–5d, so `push_integration` now runs only from Steps 3, 6a and 7 — none of which stages `docs/decisions/`, `CLAUDE.md` or `README.md`. Surviving steps deliberately **keep their old numbers** (1, 2, 3, 6a, 7, 8); the gap is cheaper than invalidating eleven prose references to "`dev:done` Step 6a". Still deletes the product-plan on project completion (all `[x]`) and closes the promoted source backlog item; Step 6a's flush re-attempts `routing: pending` items before writing new ones and carries the forward-defensive buffered plugin-scope routing branch |
| `dev:fix` | plugins/dev/skills/fix/SKILL.md | The fast path — grounded change from request to open PR in one unattended run, with no cycle artifacts (no worktree, `spec.md`, `state.json`, or decision log); escalates to `/dev` at 2+ unresolved decisions per the three-part counting rule, where a choice settled by convention counts as zero. `/dev:fix merge` — the bare token only, never prefix-matched — is the deliberate second invocation that merges and cleans up; its tail is idempotent and re-runnable. Operates on the primary checkout, and carries the repo's second guarded `PRIMARY` derivation plus an anchored `owner/name` slug allowlist. Mirrors `dev:pr` Step 4 and `dev:done` Step 2/7, which stay canonical; the duplication is named at both ends. Now also the **entry-adapter host**: a four-way argument parse (`merge` / `linear` / `backlog` / free text) whose adapter dispatch turns on whether the next token **identifies** something rather than on argument length, Step 2a's Resolve + Pre-lane hooks, a Post-PR hook, and the tail's Closeout hook — the backlog one closing its item via a marked mirror of `dev:debt` Step 6 step 4. Branch names bind to `BRANCH_NAME` (three shapes, only one `fix/`-prefixed). Now also runs **both pre-PR checks**: a build check (canonical B1–B5/O1–O3 detection, mirrored by `dev:validate` Step 5b) under one rule where a failing build **or** suite stops the lane before the PR, and a `### Review` segment dispatching **both** reviewers **issued together** — `/dev:review diff "$AUDIT_BASE"` beside `/dev:secure diff "$AUDIT_BASE"` (origin-qualified, no `<tree>` so both default to `$PRIMARY`) — under **one** bound governing both: on a P1/P2 from either it fixes once, cold re-reviews that fix's own diff, and opens the PR only on a clean re-review — a marked mirror of `dev:validate` Step 4 step 8 with `loops_max` pinned to 1. The lane passes no artifact paths, so the code reviewer's spec-comparison and plan-coverage bullets come back **`not run`** — a per-bullet status inside a delivered report, distinguished in-file from the whole-review `not run — <reason>` outcome that stops the lane. The PR body's `## What was verified` carries a `code review:` line beside `security:`, and `### The rigor floor` a matching bullet. Now also carries `### Reconcile docs prose` — a marked mirror of `dev:done` Step 4a placed **before** both reviews so its edits are inside the audited diff, with three named divergences (no `debt-pending.md` buffer, no mode split, and the Component Registry table **is** in its scope, since the lane has no Step 4) |
| `dev:init` | plugins/dev/skills/init/SKILL.md | Sets up /dev workflow infrastructure in a repo |
| `dev:migrate-tracker` | plugins/dev/skills/migrate-tracker/SKILL.md | One-shot per-repo migration of the retired aggregate `docs/dev/tech-debt.md` into the per-item `docs/backlog/` store — carries the retired source format as §The Legacy Format (L1–L8), delegates store setup to `dev:init`, takes one confirmation on a slug+scope table, routes `scope: plugin` items per §P9, and retires the tracker only on a clean five-bucket reconciliation; a one-line no-op where no tracker exists |
| `dev:plan` | plugins/dev/skills/plan/SKILL.md | Stage 3 — transforms spec + design into a build plan; Step 7a cold-reviews the plan via a fresh subagent (spec-coverage / sequencing / interface lenses) before the gate. Step 8's resume line + `Worktree:` line print **below** the `When approved` state write — a **mirror** of `dev:spec` Step 13's canonical gate resume-block ordering, differing by absence: this gate prints no autopilot offer and carries none of the three explanatory paragraphs. Step 7a's **Blocker definition is deliberately unchanged** by the challenger-economics cycle and now carries adjacent prose naming why it diverges from `dev:spec` Step 12a's build-breaking bar — the plan's three lenses are mechanical where the spec's are interpretive, so rewording a precise mechanical test into build-breaking language would only make it vaguer. Its counter semantics are a **mirror** of Step 12a's canonical `applied` / `applied_concerns` pair, restated in full rather than referenced, with the matching Path A line at Step 8 |
| `dev:pr` | plugins/dev/skills/pr/SKILL.md | Stage 6 — opens a pull request, then **records the cycle's own reasoning into that same PR**. Step 4 is `state.json.linear_issue`'s **first reader** and emits the `Closes [<id>](<url>)` lead line on a Linear-sourced cycle; it also carries the **idempotent-resume re-entry rule** (`artifacts.pr_url` set → skip `gh pr create`, reuse the number, never stop), which closed `debt-autopilot-pr-re-entry-not-idempotent` and is what lets `dev:autopilot` exempt no row stage. Step 5 splits into a state write, four relocated sub-steps, and one push: **Step 5a** Component Registry (feature-only, **canonical** for that table), **Step 5b** Reconcile Docs Prose (feature-only, **canonical**, mirrored by `dev:fix`'s `### Reconcile docs prose` with D1/D2/D3), **Step 5c** Generate Decision Log (template byte-unchanged; derives `<pre-merge-sha>` itself, since its old producer stayed in `dev:done`), **Step 5d** Run `dev:reflect` steps 1–6. The block sits **between** the state write and the push deliberately — after, so `pr_created`/`pr_number` are readable; before, so every commit lands in PR #N's diff. Every sub-step commit is guarded against an empty stage for re-entry. These four commits are reviewed by the human on the PR and by **no** automated reviewer, `dev:validate` having already run |
| `dev:reflect` | plugins/dev/skills/reflect/SKILL.md | Retrospective — **invoked from `dev:pr` Step 5d**, so it runs pre-merge on the feature branch and its output lands in the cycle's own PR. Step 5 appends `## Retrospective` to the decision log at a `$WORKDIR`-prefixed path, is **idempotent** (replaces from the **last** such heading, not the first — an earlier section can legitimately quote the literal string — and trims trailing blanks so re-entry accumulates nothing), and commits pathspec-scoped under an empty-stage guard; its bare `git push` is correct on that route and deliberately skipped on the standalone route. Step 6's skill-update gate is **three-way** — `backlog` / `debt` / `fix now` — where the first two act on nothing at the time and `fix now` is opt-in by name because it opens a second PR from a base lacking this cycle's edits; the carrying-cost test gates **both** recording choices and the body shape branches on type per the contract's two triples. Skill-edit path discovers the plugin source repo portably and §P9-validates an explicit `owner/name` PR target. Step 4's user-observation turn also runs on a handed-off cycle (`handoff_at` set) |
| `dev:shape` | plugins/dev/skills/shape/SKILL.md | Stage 2 — produces design.md with user flows; Step 11 prints the autopilot handoff offer beside the `/dev:plan` resume command, in a resume block that sits **below** the `When approved` state write — a **mirror** of `dev:spec` Step 13's canonical gate resume-block ordering, still citing it as canonical. One divergence: this gate's **Design Status confirmation** sits above the write, so the offer cannot pre-empt it |
| `dev:spec` | plugins/dev/skills/spec/SKILL.md | Stage 1 — builds the feature specification; Step 12a cold-reviews it via a fresh subagent before the gate; seeds `validate.loops_max` tier-correctly at state init; writes product-plans to the durable `docs/dev/product-plans/<slug>.md` and sets the `backlog → product-plan` promotion back-link (Steps 2/4); Step 13's gate prints the autopilot handoff offer only when the next stage is an execution stage (Plan or Build), and is **canonical** for the **gate resume-block ordering** — gate body → wait → approve → state write + commit → *then* the resume block (resume line, offer, `Worktree:` line last), mirrored by `dev:shape` Step 11 and `dev:plan` Step 8. Moving the print below the write is what closes the paste-before-approving path **without a guard**, honoring the anti-coupling constraint rather than overriding it: it removes the state a guard would have inspected; Step 1 accepts `/dev:spec linear <issue-id>` — the escalation target `dev:fix` prints — pre-filling confidence dimensions per §A5, deriving the cycle slug per §A6, and writing `linear_issue` `(writes: both)`. Step 12a's **Blocker is now a two-member class** — (a) build-breaking, a builder following the spec literally ships something broken, plus (b) the right-sizing criterion carried over so the **Scope-blocker exception keeps working unchanged** — with Concern widened to absorb the rest; both carry worked examples, and the file's `Verdict format` example was corrected, since it had classified a right-sizing finding as a Concern; the header line is now **defined in prose** rather than inferred from that example — it tallies every finding per lens (`⛔N`/`⚠️N`, `✅` only on a lens that found nothing), so a concern-only verdict can never render as all-clear. The autopilot loop's exit is a **consequence** of those definitions plus the existing "concerns never extend the loop" rule — **no exit step, exit test, or second severity concept** was added. Step 12a also carries the **drafting-history prohibition** (a revision replaces text rather than narrating drafts; `## Out of Scope` product reasoning is explicitly unaffected) and is **canonical** for the **errored-dispatch rule**: no `loops_run` advance, `blockers`/`concerns` set `null` (a third value, distinct from `0`), one retry, second error STOPs — **not mode-split**, matching `dev:validate`'s "A reviewer that cannot run stops the stage," recovered in standard mode through Step 1's existing resume-mid-approval check, with **no** "could not run" variant of the Step 13 gate. Step 12a is likewise **canonical** for the `applied` / `applied_concerns` counter pair (`dev:plan` Step 7a mirrors it), whose Path A line at Step 13 is what makes the key `(writes: both)` rather than autopilot-only |
| `dev:review` | plugins/dev/skills/review/SKILL.md | Report-only code and document review, sibling of `dev:secure` — the reviewer half of the reviewer/orchestrator split, extracted from `dev:validate` Step 2. Two **explicit** modes, each carrying its own severity table adjacent to its checklist: `/dev:review diff <base> [<tree>] [<artifact-path>…]` (the six code bullets, incl. the repo's sole `Config contract` check) and `/dev:review docs <paths>` (the five document-review bullets + the Architecture severity mapping, which lives here and nowhere else). A bare `/dev:review` is an **error** — the two modes' P1 means different things. `diff` mode's binding is **positional and fixed** (token 2 base, token 3 *always* tree, artifacts from token 4), and it is **canonical** for `<tree>` resolve-and-validate (absolute-path allowlist → `--is-inside-work-tree` → stop, never fall back), mirrored by `dev:secure`. Artifact paths are caller-supplied absolute paths, never discovered; with none — the `dev:fix` route — the spec-comparison and plan-coverage bullets report **`not run`**, never clean. Its `## Cold dispatch` is the **canonical** statement of the plugin's cold-review discipline, cited by `dev:secure`, `dev:spec` Step 12a, `dev:plan` Step 7a, `dev:validate` Step 2 and Step 4 step 8, and `dev:fix`. Carries the repo's **fourth** guarded `PRIMARY` derivation, with the non-empty guard, so `debt-primary-cd-failure-unchecked`'s count does not grow |
| `dev:secure` | plugins/dev/skills/secure/SKILL.md | On-demand security review outside the pipeline — `/dev:secure` audits the whole project, `/dev:secure diff [<base>] [<tree>]` audits the current diff. **Report-only: writes nothing**, so capturing a deferred finding is the caller's job under its own rigor floor. Consumes `dev:validate` Step 3's P1/P2/P3/Nit vocabulary rather than defining a second scheme. Now runs **cold** via a marked mirror of `dev:review`'s `## Cold dispatch`. The `diff` verb takes an optional `<tree>` (base first, tree second, so existing callers keep their meaning) with its own absolute-path allowlist and a **no-fallback stop**; the tree rule is scoped per-verb rather than blanket, and the whole-project verb keeps `$PRIMARY` as a **documented refusal** plus disclosure. Both report headers name `**Tree audited:**` as a path — replacing the whole-project verb's old ambiguous `**Scope:**` field. Pass C is stated as a **superset** of the bullets `dev:validate` used to carry (the old "adds no new vector" claim was false), now naming **CSRF** and **server-side template injection**. Carries the repo's third guarded `PRIMARY` derivation, an anchored base-ref allowlist plus `--end-of-options` against argument injection into `git diff`, and is **canonical** for the empty-diff reporting rule. Replaces the retired `security-review.md` and `security-review-diff.md` |
| `dev:validate` | plugins/dev/skills/validate/SKILL.md | Stage 5 — code review and security check. Step 2 is now an **orchestrator only**: it carries none of its three former checklists and no Architecture severity table, and instead dispatches `/dev:review diff` + `/dev:secure diff` (feature cycles, both passed `"$WORKDIR"` and the artifact paths, **issued together**) or `/dev:review docs` with absolute paths (architecture cycles), citing `dev:review`'s `## Cold dispatch` rather than restating the discipline; the architecture-cycle security carve-out is preserved verbatim. **A reviewer that cannot run stops the stage** — recorded in `validation.md`'s existing fields (`Final status: stopped` + `## Notes`, no new section), `stage` un-advanced, and named in `dev:autopilot`'s stop list. The fix loop cold re-reviews each loop's own fix diff (Step 4 step 8, **canonical** — `dev:fix`'s `### Review` mirrors it) before it may exit, and its fix-diff checklist is the one **named exception** to *an orchestrator never defines a checklist*; the step states the healthy-path shell exit-code rule once for fix authors. Step 4 step 3b requires any claim about observable command/tool behavior to be **measured** before the fix asserting it is committed. New **Step 5b build check** (mirror of `dev:fix`'s canonical detection; a failing build stops the stage before `dev:pr` and is an autopilot blocker) with a `## Build` section in `validation.md`. The architecture-cycle security carve-out now records its reasoning rather than asserting it. Step 5a's flush writes `severity: P3` and **only** that value — a nit-sourced item is still recorded when it passes carrying cost, but with no `severity` field — and applies the contract's *State the cost* second half to both its `P3 Open` and `Nits Surfaced` lists. New **step 3c** re-syncs prose around any fenced code block a fix edits, bounded to the **smallest enclosing heading** (whole-file fallback where none), reconciled inside the same loop commit; its trigger is the relation *does this English still describe the block* rather than a word list, its position is pinned so step 4's P3 circuit breaker cannot switch it off, and steps 4 and 5 re-enter it. Step 4 states the composition: a re-sync edit is defect-class but never a step-4 P3, so 3c adds a **trigger** for finding defect-class prose without widening the class. The fix-diff re-review checklist carries the matching enforcement question — inherited by `dev:fix` by reference, since that lane restates no checklist. Step 8's **same-region recurrence** rule gains a **converging-cascade exemption** (three signals: severity non-increasing and strictly below the first round *in that region*, no code changed after it, findings consequent on the same earlier edit) under which standard continues the loop and autopilot keeps fixing in-region rather than buffering out — today's behavior unchanged for the circling shape |
| `dev` shared refs — entry adapters | plugins/dev/references/entry-adapters.md | The adapter seam contract (§A1–A6) — four hook points (Resolve / Pre-lane / Post-PR / Closeout), the four-way argument tokens **dispatched by identity rather than word count** (§A2: trailing words are context; a third-or-later token makes the adapter conditional on the identifier resolving — for `backlog` against the active corpus **and** `closed/`, for `linear` against an issue-ID shape with no fetch — while `merge` keeps its stricter exact-and-alone rule), the Linear adapter (availability, fetch, asked-and-cached per-team status resolution in `config.json`, full-branch-name allowlist, `Closes` format), the backlog adapter (`<item>` resolution, refusals, and the closeout that mirrors `dev:debt` Step 6 step 4 with three named divergences), the issue→confidence-dimension mapping, and the uppercase-tolerant Linear cycle slug `dev:done` cites for interpolation safety. Loaded by `fix` and `spec`; cited by `done` |
| `dev` shared refs | plugins/dev/references/tech-debt.md | Shared tech-debt contract — per-item `docs/backlog/` store (front-matter schema, file identity, lifecycle, redesigned buffer, carrying-cost test, recurrence-merge, silent-degrade); the carrying-cost test carries a second half — **State the cost, or don't record it**, binding at every capture site and independent of review label — and `severity` accepts **`P3` only**: `Nit` is a review-time label that stops at the store boundary (a nit passing the test is recorded with no `severity` field, its body stating the gap), while `closed/` keeps archived `Nit` values as historical record; documents the live `promoted`/`promoted_to` fields + one-way `backlog → product-plan` promotion flow and ephemeral product-plan lifecycle; §P9 is the single source of truth for cross-repo routing of `scope: plugin` items (target resolution, dogfood-local, slug marker, intake dedup, degrade-to-local, retry seam, done-flush hook), and its slug allowlist is now **anchored per segment** (`^[A-Za-z0-9._][A-Za-z0-9._-]*/…`) so it actually rejects the leading dash it always claimed to — closing `debt-p9-slug-regex-allows-leading-dash` (P2). `dev:pr` joins the producing-stage rosters, Step 5b being a buffer writer. Loaded by `init`, `build`, `validate`, `reflect`, `done`, `debt`, `spec` |
| `writing:humanize` | plugins/writing/skills/humanize/SKILL.md | AI pattern detection and human voice rewriting (voice-neutral; no personal-voice coupling) |
| `writing:voice-extractor` | plugins/writing/skills/voice-extractor/SKILL.md | Extracts a person's writing voice into a reusable per-person voice skill; offers a `Writing voice:` pointer |
| `writing:linkedin` | plugins/writing/skills/linkedin/SKILL.md | LinkedIn writing with an up-front message/post/article format gate |
| `writing:email` | plugins/writing/skills/email/SKILL.md | Personal email writing |
| `writing` shared refs | plugins/writing/references/channel-best-practices.md · voice-resolution.md | Voice-neutral channel best-practices + the pointer→convention→default voice-resolution procedure, loaded by `email` and `linkedin` |
| `plugin-manager:add-plugin` | plugins/plugin-manager/skills/add-plugin/SKILL.md | Create and manage plugins in this repo |
| `ux-toolkit:ux-copywriter` | plugins/ux-toolkit/skills/ux-copywriter/SKILL.md | Expert UX copywriting — write, review, audit copy |
| `ux-toolkit:ux-designer` | plugins/ux-toolkit/skills/ux-designer/SKILL.md | UX strategy and visual design craft |
| `naming:craft-name` | plugins/naming/skills/craft-name/SKILL.md | Generate and evaluate names by mouth-feel, meaning, and strategic fit |
